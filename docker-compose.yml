version: '3'
services:
  app:
    image: orion87/student-registry:1.0.0
    ports:
      - "8081:8081" # Map port 8081 on the host to port 8081 in the container
    environment:
      - NODE_ENV=production
